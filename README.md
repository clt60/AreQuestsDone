# AreQuestsDone
An extremely simple addon that checks whether a given quest is completed or not.

Usage: `/aqd questID1 questID2 questID3`

### Example

Instead of `/run print(C_QuestLog.IsQuestFlaggedCompleted(88))`

Use: `/aqd 88`
